package com.yedam.reference;

// main메소드 없다.
// Friend 에는 아직 기능(함수) 안만든다.
// 복합적인 타입의 데이터를 하나의 변수에 담아두고 싶을때 객체를 만든다.

// 친구: 이름(문자열), 몸무게(실수), 점수(정수) - 이렇게 3가지 속성을 가진. -> 속성을 필드라고 한다.
public class Friend {
	//필드 선언 (속성들을 정의?)
	String name;
	double weight;
	int score;
}
